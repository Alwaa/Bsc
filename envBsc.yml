name: Bsc
dependencies:
  - python=3.10
  - numpy
  - scipy
  - scikit-learn #Windows version alt?
  - matplotlib
  - pandas # Not used, but might be nice for logging
  - pip
  - pip:
    - cma
    - pymongo #for PESC with spearmint
    - -e Spearmint

# Clone Spearmint from https://github.com/HIPS/Spearmint.git
# Then the conda update will run pip install -e "Spearmint"
# Clone before the env is made
